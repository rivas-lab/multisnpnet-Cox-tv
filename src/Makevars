UNAME_S := $(shell uname -s)
ifeq ($(UNAME_S),Linux)
PKG_CXXFLAGS = -I/usr/local/include/eigen $(SHLIB_OPENMP_CXXFLAGS)
endif

ifeq ($(UNAME_S),Darwin)
PKG_CXXFLAGS = -I/usr/local/include/eigen -Xpreprocessor $(SHLIB_OPENMP_CXXFLAGS)
endif
